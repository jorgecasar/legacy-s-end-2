feat: Setup foundational configuration for E2E testing

- Installed `playwright` and `@cucumber/cucumber` as dev dependencies.
- Created `playwright.config.ts` at the root, configured for browser execution.
- Created `cucumber.js` (or equivalent config) to define feature paths and step definitions.
- Established the folder structure for E2E tests (`e2e/features` and `e2e/steps`).
