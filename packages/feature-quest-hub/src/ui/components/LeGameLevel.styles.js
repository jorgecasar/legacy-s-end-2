import { css } from "lit";

export const gameLevelStyles = css`
  :host {
    display: block;
    position: relative;
    width: 100%;
    height: 100%;
    min-height: 100vh;
    background-color: var(--wa-color-surface-default);
  }
`;
